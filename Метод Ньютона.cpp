#include <iostream>
#include <cmath>

using namespace std;

double f(double n) {
    return pow(n, 3) + 47 * n - 15;  //уравнение
}
double fp(double n) {
    return 3 * pow(n, 2) + 47;      //производная
}

int main()
{
    float a, b, e, x;
    cin >> a >> b >> e;
}
